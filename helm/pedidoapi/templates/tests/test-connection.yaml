apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pedidoapi.fullname" . }}-test-connection"
  labels:
    {{- include "pedidoapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pedidoapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
